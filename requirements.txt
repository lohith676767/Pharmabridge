fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
requests>=2.31
pydantic>=2.6
pypdf>=4.0
pillow>=10.0
pytesseract>=0.3.10

# The legacy Streamlit prototype (app.py) is not part of the deployed web
# app. Install it separately if you want that UI: pip install streamlit
